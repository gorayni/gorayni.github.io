# Personal Webpage

Update JS and CSS using bower

```Shell
bower-installer
rm -rvf bower_components
```

Test Jekyll website

```Shell
rm -rvf _site; bundle exec jekyll serve --watch --incremental
```
